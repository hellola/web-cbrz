h1= title
script(src='http://localhost:3000/socket.io/socket.io.js')
script 
    var socket = new io.Socket(null,{port:3000,rememberTransport:false}); 
    socket.connect();
    socket.on('message', function(json){ 
        console.log(json);
        if(json.extraction == 'complete'){
            var temp = 'http://localhost:3000/viewImage/'+encodeURI(json.comicName)+'/'+encodeURI(json.firstFile);
            console.log(temp);
            $(".placeholder").html('<img src=' + temp  +'>'); 
        };
    });
p #{title}
br/
a(class='prev',fileName=firstFile,bookName=currentBook) Previous Page
|
a(class='next',fileName=firstFile,bookName=currentBook) Next Page 
br/
div(class='placeholder')
    != firstFile
       img(src='/viewImage/'+currentBook+'/'+firstFile)
br/
a(class='prev',fileName=firstFile,bookName=currentBook) Previous Page
|
a(class='next',fileName=firstFile,bookName=currentBook) Next Page 
br/
